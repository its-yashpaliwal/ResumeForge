spring.application.name=resume-forge-backend

# File upload limits (already set in pom.xml too – double safety)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB

# Pretty JSON when you open in browser
spring.jackson.serialization.indent-output=true